<robo:Layout
    xmlns="http://www.w3.org/1999/xhtml"
    xmlns:robo="http://robojs.org/schema">

    <robo:TextInput
        value="{Binding messageInput}" />

    <robo:Button
        text="Save"
        enabled="{Binding canSave}" />

    <div><robo:Label text="{Binding messageFeedback}" /></div>

</robo:Layout>
