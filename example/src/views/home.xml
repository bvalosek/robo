<robo:Layout
    xmlns="http://www.w3.org/1999/xhtml"
    xmlns:robo="http://robojs.org/schema">

    <robo:TextInput
        type="text"
        value="{Binding messageInput}" />

    <robo:Button
        command="saveMessage"
        enabled="{Binding canSave}">
        Save
    </robo:Button>

    <p>
        <robo:Label text="{Binding messageFeedback}" />
    </p>

</robo:Layout>
