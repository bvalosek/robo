<Layout xmlns="http://robojs.org/schema">

    <Button
        disabled="{Binding disableSubmit}"
        text="{Binding submitCaption}" />

    <TextInput
        value="{Binding text}"
        placeholder="type something" />

    <Label
        text="{Binding message}" />

    <div>
        <p>This is some random HTML stuff</p>
    </div>

</Layout>
