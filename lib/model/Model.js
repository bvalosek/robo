var ObservableObject = require('../event/ObservableObject');

module.exports = Model = require('typedef')

// Data model. Something that can be synced back to the server effectively
.class('Model') .extends(ObservableObject) .define({

});
