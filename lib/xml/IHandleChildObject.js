module.exports = IHandleChildObject = require('typedef')

// Interface for letting us do stuff with the child object in an XML document
.interface('IHandleChildObject') .define({

    handleChildObject: function(obj) {}

});
